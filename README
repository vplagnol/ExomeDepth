RA call to R as show below:

R < create_lib.R --no-save

should create the relevant version of the ExomeDepth package, with the version number defined by doc/DESCRIPTION
The create_lib.R must also be changed to reflect the different version number I think.

