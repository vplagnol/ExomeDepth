\name{TestCNV}
\alias{TestCNV}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
  Computes the Bayes Factor in favour of a CNV defined by position and type.
}
\description{
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
}
\usage{
TestCNV(x, chromosome, start, end, type)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{x}{
    ExomeDepth object containing the likelihood information.
}
  \item{chromosome}{
%%     ~~Describe \code{chromosome} here~~
}
  \item{start}{
%%     ~~Describe \code{start} here~~
}
  \item{end}{
%%     ~~Describe \code{end} here~~
}
  \item{type}{
%%     ~~Describe \code{type} here~~
}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
%%  ~~who you are~~
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
structure(function (x, chromosome, start, end, type) 
standardGeneric("TestCNV"), generic = structure("TestCNV", package = "ExomeDepth"), package = "ExomeDepth", group = list(), valueClass = character(0), signature = c("x", 
"chromosome", "start", "end", "type"), default = `NULL`, skeleton = function (x, 
    chromosome, start, end, type) 
stop("invalid call in method dispatch to \"TestCNV\" (no default method)", 
    domain = NA)(x, chromosome, start, end, type), class = structure("standardGeneric", package = "methods"))
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
